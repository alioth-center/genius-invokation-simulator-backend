kind: pipeline
type: docker
name: build

clone:
  disable: false
  depth: 1

steps:
  - name: build
    image: golang:1.19
    pull: if-not-exist
    commands:
      - go build .

  - name: unit test
    image: golang:1.19
    pull: if-not-exist
    commands:
      - go test ./...

  - name: benchmark test
    image: golang:1.19
    pull: if-not-exist
    commands:
      - go test -v -bench=./ -run=^$

  - name: notification
    image: serialt/drone-feishu-message
    pull: if-not-exists
    settings:
      token:
        from_secret: notification_token
      sha_link: true
    when:
      status: [ failure, success ]